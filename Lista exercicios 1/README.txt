Listas de exercicios:

Lista 1:

EX 1: GLSL é uma linguagem de programação baseada em C/C++ usada para escrever shaders, e seus pipelines graficos atuais usam o vertex shader e o fragment shader.

EX 2:
o armazenamento de vértices é feito usando buffers de memória da GPU para garantir um alto desempenho.
Sendo eles o VBO, VAO e EBO

EX 3:

VBO: É onde os dados dos vértices (posições, cores, coordenadas de textura) são armazenados.

VAO: Serve como um caixa que armazera os estados e configurações dos atributos dos vértices armazenados nos VBOs.


EX 6:

Feito baseado no video postado no moodle,  para criar o OCTAGONO ou o PENTAGONO basta mudar o número de nPoints para 5 ou 8 ou o circulo colocando 20.

EX 8 Baseado no video